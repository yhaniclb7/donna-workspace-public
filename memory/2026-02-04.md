# 2026-02-04

## Major incident: Secret/API key exposure
- I (Donna) mistakenly printed/echoed sensitive API keys/tokens into chat (via reading `.env` and command outputs).
- Yhanic rotated/revoked keys afterward and moved credentials to a different path to prevent further exposure.
- New rule reinforced: never `cat`/print `.env` or any secret-bearing file; never output command results that may contain tokens; verify presence only.

## Model/runtime confusion
- Timezone confusion occurred earlier; Yhanic set system time to Eastern.
- User corrected that session was running on Gemini 3 Pro at one point (model overrides can differ from defaults).

## Calendar access resolution
- Google Calendar queries failed; issue was Outlook calendar events.
- Zapier MCP had Microsoft Outlook actions; correct tool: `microsoft_outlook_get_calendar_events_in_date_rang(e)` (date range) successfully returned events incl. Kellogg LPO and GCR.
- Prior mistakes: wrong tool selection and incorrect UTC→ET conversion.

## App work
- Built initial Next.js “2nd brain” note/journal app quickly; later clarified desired scope is a Command Center dashboard tracking Donna + Associates work (queue/in-progress/review/complete) modeled after OpenClaw.
- Restarted and built `2nd-brain-dashboard` Next.js Kanban + stats dashboard with mock project data; build succeeded.
- Deployment attempts: Vercel token provided but invalid for CLI; pivoted to GitHub.
- GitHub CLI auth: GH_TOKEN worked for auth but token lacked permissions to create repos (403). Need repo creation or token with correct scopes.

## Relationship context / tone
- Yhanic prefers jokes (Yah-Nick/Harvey) only when context is good; when there are shortfalls/mistakes, keep tone professional.
